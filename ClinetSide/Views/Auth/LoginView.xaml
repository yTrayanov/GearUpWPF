<local:AuthControl x:Class="ClientSide.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientSide.Views.Auth"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock FontWeight="Bold" FontSize="26" Margin="10" TextAlignment="Center">Sign in</TextBlock>
            <StackPanel>
                <Label Content="Username" />
                <TextBox Name="tbUsername"/>
                <Label Content="Password" />
                <PasswordBox Name="tbPassword" />
            </StackPanel>
            <Button Name="btnLogin" Width="150" Margin="15" Click="btnLogin_Click">Sign in</Button>
        </StackPanel>
    </Grid>
</local:AuthControl>
